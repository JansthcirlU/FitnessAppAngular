import { Equipment } from "../equipment-base";

/**
 * Summary of a piece of equipment (any type)
 * for displaying purposes.
 */
export interface EquipmentSummary {
    name: string;
    summary: string;
}